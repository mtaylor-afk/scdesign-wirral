# SC Design Wirral — Cloudflare Pages redirects
# Copied verbatim into the static export (out/_redirects) at build time.
# Format:  /old-path   /new-path   301
#
# Populated during the redesign migration:
#  - WS3: removed/renamed service URLs (permitted-development, LDC, conservation-area → guides)
#  - WS7: retired templated area pages → /areas hub
#
# (entries added as those workstreams land — keeping this scaffold so the
#  redirect map is version-controlled from the start of the rebuild.)

# --- WS3: retired service pages (content now lives in the guides) ---
/services/permitted-development-wirral            /guides/permitted-development-rights-wirral       301
/services/lawful-development-certificate-wirral    /guides/lawful-development-certificate-explained  301
/services/conservation-area-design-wirral          /guides/conservation-area-extensions-wirral       301
/services/planning-building-regulations            /services                                         301

# --- /llm.txt compatibility → canonical machine-readable AI summary ---
/llm.txt   /llms.txt   301
